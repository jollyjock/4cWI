import React, { Component } from 'react';

export default class TaskAdder extends Component {
    render() {
        return (
            <div>
                <input type="text"/>
                <button>Save</button>
            </div>
        );
    }
}